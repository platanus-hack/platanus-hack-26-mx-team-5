<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet para el worker; mic para STT; ubicación para GPS (RAG/SceneJSON). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Bluetooth para el audio/stream de las gafas (HFP + DAT). -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- La cámara real es la de las gafas (DAT), no la del teléfono. -->

    <application
        android:label="Puente"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight">

        <!-- App ID de Meta Wearables (inyectado desde build.gradle.kts). -->
        <meta-data
            android:name="com.meta.wearable.APP_ID"
            android:value="${metaAppId}" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
